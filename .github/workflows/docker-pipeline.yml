name: hello-world-meghasree
on: push
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print your name
        run: echo "Hello World - Megha sree"

      - name: Checkout
        uses: actions/checkout@v4

      - name: Docker Build
        run: docker build -t meghasree/ml-model-22it020 .

      - name: Docker Images
        run: docker images
        
